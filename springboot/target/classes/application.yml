server:
  port: 9090

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pp0alm?serverTimezone=GMT-5
    username: root
    password: password  # my password

mybatis:
  mapper-locations: classpath:mapper/*.xml
